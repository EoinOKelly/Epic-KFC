# Network Architecture

The canonical network architecture document is:

```text
docs/architecture/network_architecture.md
```

This security-path file is kept as a compatibility pointer because an earlier draft lived under `docs/security`.
